name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write
  
jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pylint pytest pre-commit

      - name: Run pre-commit (allow fixes)
        id: precommit
        run: |
          pre-commit run --all-files || true

      - name: Commit pre-commit fixes
        if: github.event_name == 'push'
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            # Stage everything EXCEPT workflows
            git add .
            git reset .github/workflows/

            if [[ -n "$(git diff --cached)" ]]; then
              git commit -m "chore: apply pre-commit fixes [skip ci]"
              git push
            else
              echo "Only workflow files changed; skipping commit"
            fi
          else
            echo "No changes to commit"
          fi
      - name: Re-run pre-commit (verify clean)
        run: |
          pre-commit run --all-files

      - name: Run pylint
        run: |
          pylint $(git ls-files 'app.py') --fail-under=9.0
