
services:
  flask:
    build: .
    container_name: planner_flask
    ports:
      - 5000:5000
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
    volumes:
      - ./app:/app
    restart: unless-stopped
